# 2026-06-11 开发日志

## 已完成

- 排查昨天未完成 To do 未滚动到今天的问题。
- 确认用户数据库中存在 `type = todo` 但 `status = NULL` 的历史异常数据，导致后端 `status = pending` 的逾期滚动条件未命中。
- 修复 To do 状态切换逻辑：已完成事项再次点击对勾时回到 `pending`，不再写入 null。
- 新增 To do 状态迁正逻辑：数据库初始化和旧 `localStorage` 数据导入后，自动将异常空状态 To do 迁正为 `pending` 或 `done`。
- 增强逾期滚动兼容性：历史空状态且未完成的过期 To do 也会滚动到今天，并设置红色标记所需的 `rolledOverFromDate`。
- 为状态切换、历史空状态迁正、历史空状态逾期滚动补充 Rust 单元测试。
- 修复应用跨午夜保持运行时“今天”不会更新的问题：前端每分钟检查本地日期变化，跨天后自动重新加载记录并触发逾期滚动。
- 更新 `docs/data-model.md`，明确 To do 状态不能为 null。
- 验证当前用户数据库：`vibe coding` 已从 `2026-06-10` 滚动到 `2026-06-11`，状态为 `pending`，`rolled_over_from_date` 为 `2026-06-10`。
- 运行 `cargo test` 通过：3 个 Rust 单元测试全部通过。
- 运行 `npm.cmd run build` 通过。
- 运行 `npm.cmd run tauri -- build` 通过，生成最新 exe、MSI 和 NSIS 安装包。

## 待办事项

- 暂无。

## 问题/风险

- 当前 PowerShell 会话可能不会自动带上 `Z:\dev-tools` Rust 工具链路径，运行 Rust/Tauri 命令时需要临时设置环境变量。
- 需要确认长时间运行时的日期刷新在真实跨午夜场景中表现稳定；本轮通过代码路径和构建验证覆盖。

## 下一步

- 提交并推送本轮修复。

# 2026-06-11 开发日志

## 已完成

- 排查昨天未完成 To do 未滚动到今天的问题。
- 确认用户数据库中存在 `type = todo` 但 `status = NULL` 的历史异常数据，导致后端 `status = pending` 的逾期滚动条件未命中。
- 修复 To do 状态切换逻辑：已完成事项再次点击对勾时回到 `pending`，不再写入 null。
- 新增 To do 状态迁正逻辑：数据库初始化和旧 `localStorage` 数据导入后，自动将异常空状态 To do 迁正为 `pending` 或 `done`。
- 增强逾期滚动兼容性：历史空状态且未完成的过期 To do 也会滚动到今天，并设置红色标记所需的 `rolledOverFromDate`。
- 为状态切换、历史空状态迁正、历史空状态逾期滚动补充 Rust 单元测试。
- 修复应用跨午夜保持运行时“今天”不会更新的问题：前端每分钟检查本地日期变化，跨天后自动重新加载记录并触发逾期滚动。
- 更新 `docs/data-model.md`，明确 To do 状态不能为 null。
- 验证当前用户数据库：`vibe coding` 已从 `2026-06-10` 滚动到 `2026-06-11`，状态为 `pending`，`rolled_over_from_date` 为 `2026-06-10`。
- 运行 `cargo test` 通过：3 个 Rust 单元测试全部通过。
- 运行 `npm.cmd run build` 通过。
- 运行 `npm.cmd run tauri -- build` 通过，生成最新 exe、MSI 和 NSIS 安装包。
- 按用户反馈处理左上角品牌图标白边：移除 `.brand-mark` 的白色渐变底和白色内高光，仅保留透明 PNG 图标与轻量蓝色阴影。
- 清理 `logo.png`、`logo-brand.png`、`src/assets/frostnote-icon.png`、`src-tauri/icons/frostnote-icon.png`、`logo.ico`、`frostnote-shortcut-icon.ico`、`src-tauri/icons/icon.ico` 外圈半透明白色残留像素，避免在浅蓝面板上出现明显白圈。
- 更新 `docs/design-guidelines.md`，明确应用内品牌图标容器不得额外叠加会形成白边的白色底色或高光。
- 进一步移除品牌图标外部阴影，并将图标外缘浅白高光压成浅蓝色，保留内部雪花和便签纸白色主体。
- 重新运行 `npm.cmd run build` 和 `npm.cmd run tauri -- build` 均通过；截图验证左上角品牌区不再出现白色底/白圈。
- 刷新桌面快捷方式 `FrostNote.lnk` 的图标引用。
- 开始 Phase 9：账号与云同步第一版。
- 新增云同步计划文档 `docs/cloud-sync-plan.md`，并在实施前完成 Git checkpoint：`1f0b9c8 docs: checkpoint before cloud sync work`。
- 新增 Supabase 云端 schema 文档 `docs/supabase-schema.sql`，包含 `records` 表、索引和 RLS policy。
- 新增 `.env.example`，并更新 `.gitignore` 忽略真实 `.env` 和 `.env.local`。
- 安装 `@supabase/supabase-js`，新增 Supabase client 包装；未配置环境变量时应用保持本地模式。
- 安装 `vitest` 和 `@playwright/test` 作为前端测试/交互验证工具。
- 本地 SQLite 增加云同步基础字段：`deleted_at` 和 `sync_status`，支持旧库自动迁移。
- 删除记录从物理删除改为软删除 tombstone，普通 UI 列表默认隐藏已删除记录。
- 新增同步相关 Tauri 命令：读取含 tombstone 的同步记录、应用远端记录、标记同步完成。
- 新增远端记录合并逻辑：同一 `id` 采用 `updated_at` 较新的版本。
- 收窄上传范围：新增 dirty-only 同步读取能力，自动同步时只上传本地 `sync_status='dirty'` 的记录。
- 前端新增 `src/cloud/sync.ts` 和映射测试，统一处理本地 `RecordItem` 与 Supabase 行的字段转换。
- 新增 `docs/supabase-setup.md`，记录 Supabase 项目、Auth、SQL、`.env.local` 和实网验证步骤。
- 左侧栏新增账号/同步状态面板：未配置云同步时显示仅本地，配置后支持邮箱密码登录、注册、手动同步和退出。
- 更新 `docs/requirements.md`、`docs/technical.md`、`docs/data-model.md`、`docs/development-steps.md`，将账号与云同步作为 Phase 9 能力记录。
- 运行 `cargo test` 通过：8 个 Rust 单元测试全部通过。
- 运行 `npm.cmd test` 通过：3 个前端同步测试全部通过。
- 运行 `npm.cmd run build` 通过。
- 运行 `npm.cmd run tauri -- build` 通过，生成含云同步入口的最新 exe、MSI 和 NSIS 安装包。
- 使用 Playwright 截图验证 980×680 视口下账号/同步面板布局正常；交互验证点击 `To do` 后分段控件选中状态正确。
- 向用户说明真实 Supabase 项目创建流程：Dashboard 创建项目、关闭邮箱确认、执行 `docs/supabase-schema.sql`、复制 Project URL 和 publishable key、创建本地 `.env.local`，并强调不要使用 secret/service role key。
- 本地 `.env.local` 已配置 Supabase Project URL 和 publishable key；该文件被 `.gitignore` 忽略，不会提交到 Git。
- 配置 Supabase 环境变量后重新运行 `npm.cmd run build` 通过。
- 运行 Supabase 实网 smoke test：匿名 `records` 表探针成功返回 0 行，说明表/API 可访问。
- 运行 Supabase 临时账号注册测试：注册成功但未返回 session，判断邮箱确认仍开启，需关闭 Confirm email 后继续验证写入、读取、软删除和同步。
- 用户完成 Auth 设置调整后重新测试：新注册临时账号触发 Supabase `email rate limit exceeded`；尝试登录已创建测试账号仍返回 `Email not confirmed`，说明需等待邮件限流恢复后新建账号，或在 Supabase Users 中手动确认/创建已确认测试用户。
- 再次运行 Supabase 实网 smoke test：新测试账号注册后成功返回 session；RLS 下 `records` 表插入、读取、写入 `deleted_at` 软删除 tombstone、清理测试行全部通过。
- 重新运行 `npm.cmd run tauri -- build`，确认 release exe 已嵌入当前 `.env.local` 的 Supabase 配置。
- 使用 WebView2 远程调试连接真实 FrostNote 桌面窗口，完成桌面 UI 同步主链路验证：注册新测试账号、添加 `UI sync smoke` 记录、云端确认记录可见、在 UI 删除后云端确认写入 `deleted_at` tombstone，并清理测试行。
- 使用同一桌面测试账号验证云端拉回：先通过 Supabase API 预置 `Remote pull smoke` 记录，再在 FrostNote UI 点击”同步”，确认记录出现在桌面列表中；随后通过 UI 删除并确认云端 tombstone，最后清理测试行。
- 支持自定义 UID 注册：云端用户标识从 Supabase 自动生成的 UUID 改为用户在注册时选择的自定义 UID（3-30 位字母数字）。
- 新增 `public.profiles` 表，通过 `on_auth_user_created` 触发器在注册时自动填充，映射 `auth.users` UUID 到自定义 UID。
- 新增 `public.get_my_custom_uid()` RLS 辅助函数和 `public.check_custom_uid_available()` RPC，分别用于策略判定和注册前可用性检查。
- `records.user_id` 从 `uuid` 改为 `text`，直接存储自定义 UID；四条 RLS policy 全部改用 `get_my_custom_uid()` 判定。
- 前端注册表单新增用户名输入框（仅在注册模式下显示），包含格式校验和 Supabase RPC 可用性检查。
- 注册时通过 `signUp({ options: { data: { custom_uid } } })` 传入自定义 UID，登录后从 `user.user_metadata.custom_uid` 读取并用于同步。
- 更新 `docs/supabase-schema.sql`（完整重写）、`docs/supabase-setup.md`、`docs/data-model.md` 和 `docs/cloud-sync-plan.md` 中的自定义 UID 相关内容。
- 运行 `npm.cmd test` 通过：3 个前端同步测试全部通过。
- 运行 `npm.cmd run build` 通过。
- 在 Supabase 项目中执行最新 schema（含 profiles 表、触发器、自定义 UID RLS），并通过 curl 完成实网 E2E 验证：
  - `check_custom_uid_available` RPC：空闲 UID 返回 `true`，已占用 UID 返回 `false`
  - 注册时 `custom_uid` 正确写入 `user_metadata` 并由触发器自动创建 `profiles` 行
  - 插入记录时 RLS 允许匹配自身 `custom_uid` 的写入，拒绝冒充他人 UID（返回 42501）
  - 跨用户查询隔离：User B 看不到 User A 的记录，只能看到自己的
  - 软删除 `deleted_at` tombstone 正常写入
  - 测试完成后清理所有测试用户和数据
- 运行 `npm.cmd run tauri -- build` 通过，生成含自定义 UID 功能的最新 exe、MSI 和 NSIS 安装包。
- 调整云同步面板布局：登录后将同步/退出按钮移到 UID 右侧同一行；将同步消息和时间移到 header "已同步" 右侧。
- 重新运行 `npm.cmd run build` 和 `npm.cmd run tauri -- build` 通过，确认 exe/MSI/NSIS 时间戳一致。
- 按用户标注截图调整紧凑模式记录卡片布局：在缩小窗口下将事项内容区固定在卡片左侧，将 To do 对勾按钮固定到卡片右侧居中位置，避免响应式窄屏规则把对勾挤到内容下方。
- 重新运行 `npm.cmd test`、`npm.cmd run build` 和 `npm.cmd run tauri -- build` 均通过。
- 使用 WebView2 远程调试进入 FrostNote 紧凑模式并截图验证：记录内容位于左侧，对勾按钮位于事件框右侧居中。
- 继续按用户反馈调整紧凑模式已完成 To do：将完成时间从正文下方移动到 `To do` 标签左侧，同一行显示，减少卡片纵向占用。
- 重新运行 `npm.cmd test`、`npm.cmd run build` 和 `npm.cmd run tauri -- build` 均通过。
- 使用 WebView2 远程调试验证紧凑模式 DOM 坐标：完成时间位于 `To do` 标签左侧，右侧对勾按钮仍保持在卡片右侧。
- 更正完成时间位置：按用户修正要求，将已完成 To do 的完成时间调整到 `To do` 标签右侧，并同步覆盖主窗口和紧凑窗口。
- 重新运行 `npm.cmd test`、`npm.cmd run build` 和 `npm.cmd run tauri -- build` 均通过。
- 使用 WebView2 远程调试验证主窗口与紧凑窗口 DOM 坐标：`To do` 标签位于完成时间左侧，紧凑窗口右侧对勾按钮仍保持在事件框右侧。
- 修复无边框窗口最小化后无法唤出的问题：新增统一窗口恢复 helper，恢复入口先执行 native restore/unminimize，再 show 和 focus。
- 将 Windows 最小化命令从 raw `SW_MINIMIZE` 收敛为 Tauri 原生 `window.minimize()`，让任务栏还原状态由 Tauri/系统正常接管；主动恢复路径继续使用 native restore。
- 将托盘菜单“显示 FrostNote”、托盘左键点击、`Ctrl+Shift+F` 全局快捷键恢复分支统一改为使用同一恢复 helper。
- 修正全局快捷键在窗口已最小化但仍被判定为 visible 时会继续 hide 的问题：只有窗口可见且未最小化时才隐藏，否则执行恢复。
- 增加 Windows 专用前台最小化兜底检查：当窗口处于最小化且已稳定成为前台窗口约 1 秒时，自动执行统一恢复 helper，覆盖任务栏/系统激活后仍停留在 minimized 的情况；通过最小化保护位避免刚点击应用内最小化按钮时被立刻弹回。
- 在 capability 中补充 `core:window:allow-unminimize`，为未来直接调用 Tauri unminimize API 保留权限。
- 新增 Rust 单元测试锁定窗口恢复步骤顺序；先确认测试红灯，再实现后运行 `cargo test` 通过：9 个 Rust 单元测试全部通过。
- 运行 `npm.cmd test` 通过：3 个前端同步测试全部通过。
- 运行 `npm.cmd run build` 通过。
- 运行 `npm.cmd run tauri -- build` 通过，生成最新 exe、MSI 和 NSIS 安装包。
- 使用 WebView2 + Win32 状态检查验证窗口恢复：从 hidden/minimized 状态触发 `Ctrl+Shift+F` 后，窗口恢复为 visible 且不再 minimized。

## 待办事项

- 创建真实 Supabase 项目，关闭邮箱验证，并执行 `docs/supabase-schema.sql`。
- 继续补充多端冲突场景验证，确认 `updatedAt` 最新者胜出的策略在真实 Supabase 环境中表现一致。

## 问题/风险

- 当前 PowerShell 会话可能不会自动带上 `Z:\dev-tools` Rust 工具链路径，运行 Rust/Tauri 命令时需要临时设置环境变量。
- 需要确认长时间运行时的日期刷新在真实跨午夜场景中表现稳定；本轮通过代码路径和构建验证覆盖。
- 当前尚未配置真实 Supabase 项目，云同步实网流程未验证。
- 普通浏览器环境没有 Tauri `invoke`，Playwright 浏览器验证中出现的 `invoke` console error 属预期限制；真实桌面行为需通过 Tauri 构建/运行继续验证。
- 已确认 Supabase Auth 关闭邮箱确认后，新账号注册可直接返回 session；旧的未确认测试用户仍可能无法登录，可在 Supabase Users 中删除或手动确认。
- Windows 无边框窗口恢复依赖 native restore 路径，后续若继续扩展窗口行为，需要同时覆盖托盘、任务栏和快捷键入口。

## 下一步

- 自定义 UID 功能已完成 Supabase schema 执行和实网 E2E 验证，核心流程（注册设定 UID、RLS 隔离、软删除）均通过。
- 桌面端 E2E 验证可在 FrostNote 中注册新账号（带自定义 UID），添加记录后同步，并在 Supabase 控制台确认 `records.user_id` 为选择的 UID。
- 按用户要求决定是否提交 Git。
- 继续根据紧凑模式真实使用截图微调卡片间距、文字截断和右侧操作区尺寸。
- 检查紧凑模式下较长完成时间和较长内容同时出现时的换行观感。
- 完成最小化恢复修复后的 release exe 手动验证：最小化后任务栏/托盘/快捷键均应能唤回窗口。
